# Controllers

Controladores HTTP específicos devem ficar aqui quando as rotas crescerem. Nesta fase, o roteamento principal ainda delega diretamente para services/repositories em `routes/http-routes.ts` para manter a API pequena.
